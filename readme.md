## Data Structures Implemented in Typescript (specs included!)

### Getting started

1. `npm run compile` - compiles ts files into `build` directory
2. `npm test` - runs unit test suite and code coverage report